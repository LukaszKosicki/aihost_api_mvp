apiVersion: v1
kind: Service
metadata:
  name: aihost-api-service
  namespace: aihost-mvp
spec:
  selector:
    app: aihost-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
